name: Android APK Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout kode
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Namesti Android build tools
        run: |
          sdkmanager --install "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      - name: Daj pravice za gradlew
        run: chmod +x gradlew

      - name: Build vseh APK-jev
        run: |
          ./gradlew --no-daemon clean \
            :app:assembleDebug \
            :companion:assembleContact1Debug \
            :companion:assembleContact2Debug \
            :companion:assembleContact3Debug \
            :companion:assembleContact4Debug \
            :companion:assembleContact5Debug \
            :companion:assembleContact6Debug \
            --stacktrace

      - name: Pripravi APK output
        run: |
          rm -rf output
          mkdir -p output

          cp app/build/outputs/apk/debug/app-debug.apk output/app-debug.apk
          cp companion/build/outputs/apk/contact1/debug/companion-contact1-debug.apk output/companion-contact1-debug.apk
          cp companion/build/outputs/apk/contact2/debug/companion-contact2-debug.apk output/companion-contact2-debug.apk
          cp companion/build/outputs/apk/contact3/debug/companion-contact3-debug.apk output/companion-contact3-debug.apk
          cp companion/build/outputs/apk/contact4/debug/companion-contact4-debug.apk output/companion-contact4-debug.apk
          cp companion/build/outputs/apk/contact5/debug/companion-contact5-debug.apk output/companion-contact5-debug.apk
          cp companion/build/outputs/apk/contact6/debug/companion-contact6-debug.apk output/companion-contact6-debug.apk

      - name: Ustvari app-version.json
        run: |
          cat > app-version.json <<'EOF'
          {
            "versionCode": 1,
            "versionName": "1.0",
            "apkUrl": "https://github.com/trsavecdusan-wq/Nova-rehab/releases/download/novarehab-companion-latest/app-debug.apk",
            "companionApkUrls": {
              "contact1": "https://github.com/trsavecdusan-wq/Nova-rehab/releases/download/novarehab-companion-latest/companion-contact1-debug.apk",
              "contact2": "https://github.com/trsavecdusan-wq/Nova-rehab/releases/download/novarehab-companion-latest/companion-contact2-debug.apk",
              "contact3": "https://github.com/trsavecdusan-wq/Nova-rehab/releases/download/novarehab-companion-latest/companion-contact3-debug.apk",
              "contact4": "https://github.com/trsavecdusan-wq/Nova-rehab/releases/download/novarehab-companion-latest/companion-contact4-debug.apk",
              "contact5": "https://github.com/trsavecdusan-wq/Nova-rehab/releases/download/novarehab-companion-latest/companion-contact5-debug.apk",
              "contact6": "https://github.com/trsavecdusan-wq/Nova-rehab/releases/download/novarehab-companion-latest/companion-contact6-debug.apk"
            },
            "message": "Nova verzija aplikacije NovaRehab je na voljo."
          }
          EOF

      - name: Preveri seznam APK-jev
        run: |
          echo "APK datoteke za release:"
          find output -maxdepth 1 -type f -name '*.apk' -print | sort

          test -f output/app-debug.apk
          test -f output/companion-contact1-debug.apk
          test -f output/companion-contact2-debug.apk
          test -f output/companion-contact3-debug.apk
          test -f output/companion-contact4-debug.apk
          test -f output/companion-contact5-debug.apk
          test -f output/companion-contact6-debug.apk
          test -f app-version.json

          apk_count=$(find output -maxdepth 1 -type f -name '*.apk' | wc -l | tr -d ' ')
          if [ "$apk_count" != "7" ]; then
            echo "NAPAKA: output mora imeti tocno 7 APK datotek, nasel sem $apk_count"
            find output -maxdepth 1 -type f -print
            exit 1
          fi

      - name: Preveri package name companion APK-jev
        run: |
          AAPT="$ANDROID_HOME/build-tools/34.0.0/aapt"
          if [ ! -x "$AAPT" ]; then
            AAPT=$(find "$ANDROID_HOME/build-tools" -name aapt -type f | sort | tail -n 1)
          fi

          if [ ! -x "$AAPT" ]; then
            echo "NAPAKA: aapt ni bil najden"
            exit 1
          fi

          check_package() {
            apk="$1"
            expected="$2"
            actual=$("$AAPT" dump badging "$apk" | sed -n "s/package: name='\([^']*\)'.*/\1/p" | head -n 1)
            echo "$apk -> $actual"
            if [ "$actual" != "$expected" ]; then
              echo "NAPAKA: napacen package za $apk"
              echo "Pricakovano: $expected"
              echo "Dejansko:    $actual"
              exit 1
            fi
          }

          check_package output/companion-contact1-debug.apk com.novarehab.companion.contact1
          check_package output/companion-contact2-debug.apk com.novarehab.companion.contact2
          check_package output/companion-contact3-debug.apk com.novarehab.companion.contact3
          check_package output/companion-contact4-debug.apk com.novarehab.companion.contact4
          check_package output/companion-contact5-debug.apk com.novarehab.companion.contact5
          check_package output/companion-contact6-debug.apk com.novarehab.companion.contact6

      - name: Nalozi debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: NovaRehab-debug-apks
          path: |
            output/*.apk
            app-version.json
          if-no-files-found: error

      - name: Odstrani stare companion APK-je iz release
        run: |
          gh release delete-asset novarehab-companion-latest companion-debug.apk --yes || true
          gh release delete-asset novarehab-companion-latest companion-zana-debug.apk --yes || true
          gh release delete-asset novarehab-companion-latest companion-dedek-debug.apk --yes || true
          gh release delete-asset novarehab-companion-latest companion-inna-debug.apk --yes || true
          gh release delete-asset novarehab-companion-latest companion-julija-debug.apk --yes || true
          gh release delete-asset novarehab-companion-latest companion-kuma-debug.apk --yes || true
          gh release delete-asset novarehab-companion-latest companion-dusan-debug.apk --yes || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Ustvari ali posodobi release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: novarehab-companion-latest
          name: NovaRehab Companion APKs
          draft: false
          prerelease: false
          make_latest: true
          overwrite_files: true
          files: |
            output/*.apk
            app-version.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
