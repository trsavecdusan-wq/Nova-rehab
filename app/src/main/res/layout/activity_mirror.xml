<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleX="-1" />

    <Button
        android:id="@+id/btnCloseMirror"
        android:layout_width="160dp"
        android:layout_height="54dp"
        android:layout_gravity="bottom|start"
        android:layout_marginStart="20dp"
        android:layout_marginBottom="28dp"
        android:backgroundTint="#E94560"
        android:text="ZAPRI"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold" />

    <ImageButton
        android:id="@+id/btnSwitchCamera"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:background="@drawable/bg_mirror_icon_button"
        android:contentDescription="Preklop kamere"
        android:padding="14dp"
        android:src="@android:drawable/ic_menu_rotate"
        android:tint="#FFFFFF" />

    <ImageButton
        android:id="@+id/btnCaptureMirror"
        android:layout_width="84dp"
        android:layout_height="84dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="96dp"
        android:background="@drawable/bg_mirror_capture_button"
        android:contentDescription="Slikaj"
        android:padding="0dp" />

</FrameLayout>
